spring:
  datasource:
    url: jdbc:mysql://192.168.210.142:3306/cursos_db
    username: root
    password: Apr0bad0!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
springdoc:
  packages-to-scan: com.ejercicio.crud.controller
  paths-to-match: /**
server:
  port: 0
eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
