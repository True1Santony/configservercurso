spring:
server:
  port: 0
eureka:
  instance:
    prefer-ip-address: true
    hostname: 192.168.210.14
    non-secure-port: 8081
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

cursos:
  urlBase: ${URL_BASE_CURSO:http://servicio-cursos}
  username: user3
  password: user3
